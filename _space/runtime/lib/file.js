/**
 * 文件读写
 */

const fs = require('fs')

const _U = {
    /**
     * 内容写入文件
     * @param content
     * @param file
     */
    writeFile(content, file){


    }
}

module.exports = function () {
    return _U
}
